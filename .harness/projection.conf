# .harness/projection.conf — declared lane views (projection.sh reads this).
# NOTHING here is active: every line below is a commented example. Declaring the
# tester view IS defining the oracle's world — that is a per-run design decision
# the Validator settles in phase artifacts, not a default this file supplies.
# dispatch_lane.sh fails closed ("no oracle yet" / exit 66) until declared.
#
# coder-exclude: tests
# tester-include: pyproject.toml
# tester-include: docs/specs
# tester-include: factory_core/adapters.py
