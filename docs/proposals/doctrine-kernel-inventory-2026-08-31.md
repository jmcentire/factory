# DOCTRINE-KERNEL.md deletion inventory (plan 5.2 Step 1)

Survival criterion (plan): a rule survives the merge iff it is (a) enforced by a
registered mechanical check or (b) an explicit founder-ratified declaration.
DOCTRINE-KERNEL.md dies only after every S/I/E/X row below is either migrated
into the canonical (`docs/SOFTWARE-FACTORY.md` Part I) or its deletion is
explicitly founder-ratified, BY NAME. This table is a MECHANICAL keyword
first pass (all probe tokens found in the canonical = PRESENT); the migration
commit does exact wording. Nothing here takes effect from prose.

Note: the plan orders the I2/tool_policy "verified live drift" claim struck —
no such claim exists in the current DOCTRINE-KERNEL.md text (searched
2026-08-31); recorded as already-absent.

| Row | Mechanical first-pass disposition |
|---|---|
| S1 | PRESENT-in-canonical (kernel row is a duplicate; delete with ratification) |
| S2 | ABSENT (migrate into the canonical before the kernel dies, or founder-ratify the deletion by name) |
| S3 | PRESENT-in-canonical (kernel row is a duplicate; delete with ratification) |
| S4 | PRESENT-in-canonical (kernel row is a duplicate; delete with ratification) |
| S5 | PRESENT-in-canonical (kernel row is a duplicate; delete with ratification) |
| I1 | PRESENT-in-canonical (kernel row is a duplicate; delete with ratification) |
| I2 | ABSENT (migrate into the canonical before the kernel dies, or founder-ratify the deletion by name) |
| I3 | ABSENT (migrate into the canonical before the kernel dies, or founder-ratify the deletion by name) |
| I4 | PRESENT-in-canonical (kernel row is a duplicate; delete with ratification) |
| I5 | PRESENT-in-canonical (kernel row is a duplicate; delete with ratification) |
| I6 | PRESENT-in-canonical (kernel row is a duplicate; delete with ratification) |
| I7 | ABSENT (migrate into the canonical before the kernel dies, or founder-ratify the deletion by name) |
| I8 | ABSENT (migrate into the canonical before the kernel dies, or founder-ratify the deletion by name) |
| I9 | ABSENT (migrate into the canonical before the kernel dies, or founder-ratify the deletion by name) |
| I10 | ABSENT (migrate into the canonical before the kernel dies, or founder-ratify the deletion by name) |
| I11 | PRESENT-in-canonical (kernel row is a duplicate; delete with ratification) |
| I12 | PRESENT-in-canonical (kernel row is a duplicate; delete with ratification) |
| I13 | PRESENT-in-canonical (kernel row is a duplicate; delete with ratification) |
| I14 | ABSENT (migrate into the canonical before the kernel dies, or founder-ratify the deletion by name) |
| I15 | PRESENT-in-canonical (kernel row is a duplicate; delete with ratification) |
| I16 | PRESENT-in-canonical (kernel row is a duplicate; delete with ratification) |
| I17 | PRESENT-in-canonical (kernel row is a duplicate; delete with ratification) |
| I18 | PRESENT-in-canonical (kernel row is a duplicate; delete with ratification) |
| I19 | PRESENT-in-canonical (kernel row is a duplicate; delete with ratification) |
| I20 | PRESENT-in-canonical (kernel row is a duplicate; delete with ratification) |
| I21 | PRESENT-in-canonical (kernel row is a duplicate; delete with ratification) |
| I22 | PRESENT-in-canonical (kernel row is a duplicate; delete with ratification) |
| I23 | PRESENT-in-canonical (kernel row is a duplicate; delete with ratification) |
| E1 | ABSENT (migrate into the canonical before the kernel dies, or founder-ratify the deletion by name) |
| E2 | ABSENT (migrate into the canonical before the kernel dies, or founder-ratify the deletion by name) |
| E3 | ABSENT (migrate into the canonical before the kernel dies, or founder-ratify the deletion by name) |
| E4 | PRESENT-in-canonical (kernel row is a duplicate; delete with ratification) |
| X1 | ABSENT (migrate into the canonical before the kernel dies, or founder-ratify the deletion by name) |
| X2 | ABSENT (migrate into the canonical before the kernel dies, or founder-ratify the deletion by name) |
| X3 | ABSENT (migrate into the canonical before the kernel dies, or founder-ratify the deletion by name) |
| X4 | PRESENT-in-canonical (kernel row is a duplicate; delete with ratification) |

**Absent rows requiring migration or named ratification: S2, I2, I3, I7, I8, I9, I10, I14, E1, E2, E3, X1, X2, X3.**

- [ ] founder: ratify deletion of every PRESENT row (duplicates of the canonical)
- [ ] founder: per ABSENT row — migrate (default) or ratify deletion by name: ______
