# factory_core — agent entry point

The working agreement lives in [`CLAUDE.md`](CLAUDE.md); the canonical
rulebook is [`docs/SOFTWARE-FACTORY.md`](docs/SOFTWARE-FACTORY.md); terms are
defined once in [`docs/GLOSSARY.md`](docs/GLOSSARY.md). Run `make ship`
before claiming anything green. This file is a pointer, not a second copy —
do not add rules here (remediation 5.2).
