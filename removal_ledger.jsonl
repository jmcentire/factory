{"phase": "0.1", "axis": "2", "kind": "add", "subject": {"path": "harness/refusal_event_kinds.json"}, "note": "closed refusal-event kind registry (net addition, stated plainly)", "status": "landed"}
{"phase": "0.1", "axis": "2", "kind": "add", "subject": {"path": "harness/attention_gate.py"}, "note": "append_refusal_event closed writer + refusal-event subcommand (net addition; path predates the addition \u2014 tree claim is presence-only)", "status": "landed"}
{"phase": "0.2", "axis": "2", "kind": "add", "subject": {"path": "harness/record_no.sh"}, "note": "host-written terminal-NO record, sole writer of the no disposition (net addition, stated plainly)", "status": "landed"}
{"phase": "0.2", "axis": "2", "kind": "add", "subject": {"path": "harness/terminal_no_kinds.json"}, "note": "closed terminal-NO kind registry with signal/bound class split (net addition)", "status": "landed"}
{"phase": "0.3", "axis": "9", "kind": "add", "subject": {"path": "scripts/check_acceptance.py"}, "note": "acceptance instrument checker (net addition \u2014 the plan names it one of the two pure additions without which faster-and-earlier-NO is an assertion)", "status": "landed"}
{"phase": "0.4a", "axis": "2", "kind": "add", "subject": {"path": "tests/test_generation_signal_knobs.py"}, "note": "signal-knob validation forcing tests; the checks live in factory_runtime/generation.py _signal_knob_issues wired at prepare() and verify_prepared_generation(), with the schema signal object in factory_core/schemas/target_manifest.schema.json (net addition, stated plainly)", "status": "landed"}
{"phase": "0.4b", "axis": "2", "kind": "add", "subject": {"path": "factory_runtime/state.py"}, "note": "validating_pass_count host pass counter + CLI pass-count door (net addition; path predates \u2014 presence-only claim). One pass = one VALIDATING admission from the verified ledger; never a line count.", "status": "landed"}
{"phase": "0.4c", "axis": "2", "kind": "add", "subject": {"path": "harness/watchdog.py"}, "note": "signal-deadline watchdog: pass deadline (warn/terminal-NO via record_no with checked exit code) + wall-clock backstop on two-live-check stalls; knobs only from the frozen-blob CLI door (net addition, stated plainly)", "status": "landed"}
{"phase": "0.4", "axis": "2", "kind": "add", "subject": {"path": "tests/test_generation_prepare_forcing.py"}, "note": "prepare()-level forcing tests (round-4 D1/D2): knobless target refuses at readiness; a re-signed manifest refuses with target-manifest-run-digest-mismatch \u2014 red under the unwiring mutation", "status": "landed"}
{"phase": "0.5", "axis": "2", "kind": "add", "subject": {"path": "docs/proposals/phase0-sitting-sheet.md"}, "note": "gate WPX (write-path exclusivity probe, both grant directions) + the founder sitting sheet + pre-wired required-metrics floor (net additions completing Phase 0 instrumentation)", "status": "landed"}
{"phase": "1.1d", "axis": "2", "kind": "delete", "subject": {"class": "generation-freeze-then-refuse-ordering"}, "note": "refuse-then-freeze reordering (plan 1.1d): a refused attempt no longer freezes the five input blobs it will never consume \u2014 only the readiness snapshot (residual-blocker artifact, marked refused) is retained; enforced by tests/test_generation_prepare_forcing.py residual-snapshot test", "status": "landed"}
{"phase": "1.1", "axis": "2", "kind": "add", "subject": {"path": "tests/test_promotion_code_classes.py"}, "note": "promotion reason-code classification exported from promotion.py (122 codes: 25 configuration / 6 surface-scoped / 91 construction-evidence) with the bidirectional reverse forcing test \u2014 the preflight hard-NO foundation", "status": "landed"}
{"phase": "1.1", "axis": "2", "kind": "add", "subject": {"path": "factory_runtime/preflight.py"}, "note": "the feasibility preflight core (plan-named pure addition): five verdict-reachability checks, ceiling disposition, class-split hard-NO/disclosure output, tri-state could-not-check; CLI door next slice", "status": "landed"}
{"phase": "1.1c", "axis": "1", "kind": "delete", "subject": {"class": "promotion-gate-m-agent-envelope"}, "note": "agent-declared PromotionRequest.disturbed_surface_ids + candidate_receipt/candidate_receipt_evidence envelope comparison block deleted ATOMICALLY with the host-derivation replacement (derive_disturbed_surfaces over changed_paths + plan-declared surface_map); unmapped path / absent map -> implicit-Critical pseudo-surface (criticality.py unknown->Critical inherited, never re-implemented); forcing tests retargeted per the Phase 5 retirement rule (gates.tsv row M) Forcing tests: tests/test_promotion_gate.py::test_promotion_routes_unmapped_paths_to_implicit_critical, tests/test_promotion_gate.py::test_retired_agent_declared_surface_field_is_inert.", "status": "landed"}
{"phase": "1.1c", "axis": "1", "kind": "delete", "subject": {"class": "promotion-gate-build-envelope-projection"}, "note": "promotion_gate.py build branch of _envelope_projection + verify_chain_anchor's candidate_receipt grounding deleted with the same change \u2014 no build envelope exists to ground or forge; oracle/flake projections survive until Phase 4; receipt.sh's diff-to-surface machinery SURVIVES as the host-side precedent and producer Forcing tests: tests/test_promotion_gate_runtime.py::test_chain_anchor_fail_closed_when_receipt_id_not_in_chain, tests/test_promotion_gate_runtime.py::test_chain_anchor_fail_closed_when_envelope_does_not_match_chain (retargeted to the surviving oracle projection).", "status": "landed"}
{"phase": "1.5", "axis": "2", "kind": "add", "subject": {"path": "factory_core/build_plan.py"}, "note": "the three plan-named additive joins: BuildStep.delivers_verbs (verb_ids <-> steps, the build_plan.py:503 analogue), BuildStep.promises_probes (required_probe_ids <-> promised probes), CoverageTerritory.expectation_refs (territories <-> oracle_links); all digest-stable (emitted only when declared, so ratified bytes re-derive unchanged); preflight joins leg is tri-state PER JOIN \u2014 an undeclared join is NA + disclosure, loud, never a hard NO against a legacy plan and never green", "status": "landed"}
{"phase": "2.1", "axis": "6", "kind": "delete", "subject": {"class": "target-manifest-dual-digest"}, "note": "TargetManifest.content_digest (canonical re-encoding) deleted \u2014 signing a re-encoding of what the parser produced verified the parser, not the file; source_digest over the raw bytes read is THE digest; schema const factory-target-manifest/2 refuses /1 at parse (earliest firing point, fail-closed re-declaration); pair collapsed across workflow.py (single request-bound raw-byte equality = the live gate), target_state.py, generation.py (readiness/2; old retained /1 runs fail closed retroactively per the pre-0.4a signal-knob precedent), runner.py receipts + runner_failure.py + cli.py:1474 + dispatch_lane.sh (raw_digest producer); genesis artifact key target-manifest-source no longer required (old ledgers with it still load, presence-only check); retired twin fields kept as OPTIONAL deprecated schema properties so retained historical documents stay loadable \u2014 historical records are not rewritten Forcing tests: tests/test_target_loader.py::test_the_single_digest_is_over_the_raw_bytes, tests/test_target_loader.py::test_schema_1_manifest_refuses_at_parse.", "status": "landed"}
{"phase": "2.1", "axis": "6", "kind": "delete", "subject": {"class": "in-file-signature-self-claim"}, "note": "the [signature] content_digest/value/signer/algorithm in-file fields deleted from schema and loader \u2014 a file cannot vouch for itself; the block carries only key_id, the signature VALUE lives detached with the trust root, and verify_signature (DORMANT until an out-of-repo trust root exists \u2014 no doc may count it active) now receives the raw bytes, never a canonical re-encoding Forcing tests: tests/test_target_loader.py::test_in_file_content_digest_self_claim_is_refused_by_schema, tests/test_target_loader.py::test_signature_seam_verifies_over_the_raw_bytes.", "status": "landed"}
{"phase": "2.1", "axis": "6", "kind": "add", "subject": {"path": "factory_core/manifest.py"}, "note": "digest_obj hardened for injectivity over its actual input domain: allow_nan=False (NaN/Infinity emitted non-JSON tokens) and non-string dict keys refused (json key coercion let 1 and \"1\" collide into one content address)", "status": "landed"}
{"phase": "2.2", "axis": "6", "kind": "add", "subject": {"path": "factory_core/manifest.py"}, "note": "keyed core chain: Ledger(chain_key=) with hmac-sha256:<hex> entry addresses (HMAC-SHA256 over the canonical body); the PREFIX is the mode \u2014 keyed-vs-unkeyed fails at entry 0 with no mode flag; chain_key=None is deprecated migration-only and LOUD (FutureWarning) on new-ledger construction; verify_ledger threads the key; whole-history rewrite now requires the host-held key", "status": "landed"}
{"phase": "2.2", "axis": "6", "kind": "add", "subject": {"path": "factory_runtime/durability.py"}, "note": "founder-root-recoverable chain-key derivation at the durability seam: per-ledger key = HMAC-SHA256(.chain-root.key material, relative ledger path) \u2014 key-file loss degrades to re-derivation from the retained root asset, never to a permanently unverifiable ledger; absent root -> None (migration); documented fail-closed consequence if the root asset is unrecoverable; threaded at ALL FOUR construction sites (state.py, resources.py, evidence_plane.py, resume.py:150) with a mechanical enumeration forcing test (tests/test_chain_key.py) that reds on any new unkeyed site; ledger-HEAD validators (schemas $defs/ledger_head, transition_obligations._is_ledger_head) accept both prefixes while artifact content digests stay sha256-only", "status": "landed"}
{"phase": "2.2", "axis": "6", "kind": "add", "subject": {"path": "factory_runtime/projection_bundle.py"}, "note": "2.2 tail: negative-space enforcement \u2014 bundle_runner_projection REFUSES (never silently sanitizes) any .chain-root.key in a projection root; NamedSecretStore's name grammar structurally refuses the filename; forcing tests pin both. Keyed-genesis commitment: genesis schema + AuthorityPolicy gain optional chain_root_commitment (sha256 of root material); run creation refuses when the local material is absent or does not re-derive the founder-root-signed digest (workflow.py, pre-create). One-machine guard half (a): enumeration tests pin decide_promotion to exactly one caller (promotion_gate.py) and the verdict ceiling vocabulary to one defining module (verdict.py) \u2014 a second promotion machine or a forked ceiling ranking reds before it can exist", "status": "landed"}
{"phase": "0.4c-r6", "axis": "5", "kind": "add", "subject": {"path": "harness/watchdog.py"}, "note": "round-6 closures: 6-5 promote's locked flip mirrors the pre-check (any status outside open/closed refuses at rc 71 \u2014 a terminal NO recorded inside the race window stands, race forcing test drives the real window via a FACTORY_CLI wrapper); 6-2 dispatcher invocation forcing test drives the REAL run_loop and hangs red if the one signal_watchdog.check line is deleted; 6-3 consumer-side pass-unit test (50 dispatch receipt lines != passes) plus module-must-not-name-receipts-file assertion; 6-6 pass advance re-arms backstop_fired (latch is per-stall, never permanent silence); 6-7 valid-JSON-wrong-shape state degrades to watchdog_state_reset re-arm, never a crash; 6-8 consecutive-error counter escalates wake=True once at 3 and re-arms on a good observation; 6-1 e2e attempt-2 unchanged re-prepare success + frozen-deadline blob re-derivation forcing tests", "status": "landed"}
{"phase": "1.1", "axis": "2", "kind": "note", "subject": {"path": "tests/test_promotion_code_classes.py"}, "note": "round-7 census correction: the earlier add-row's '122 codes: 25/6/91' was the census AT ITS COMMIT; 1.1c (95ae363) dropped the eight Gate M envelope labels and added three derivation reports, and the path: reservation (e8d4e22) added one configuration code \u2014 census at head is 118: 26 configuration / 5 surface-scoped / 87 construction-evidence, asserted by test_class_partitions_are_disjoint_and_complete", "status": "landed"}
{"phase": "0.4-r6-tail", "axis": "5", "kind": "add", "subject": {"path": "harness/watchdog.py"}, "note": "round-6/7 tail closures: 6-11 healthy-green exemption RESTORED (plan 0.4 semantics: deadline fires only when residual blockers are PRESENT; new readiness-issues CLI door reads the retained generation-readiness snapshot; demonstrated-empty issues -> emitted exemption event, never silence; refusing door fires fail-closed); 6-4 build.signal REQUIRED at target schema/2 (knobless refuses at parse \u2014 no frozen tuple can carry undeclared knobs; verify re-check is defense-in-depth) + 8760h maximum forcing test (9000h refuses); 6-9 env-refusal upgraded from two-substring grep to AST scan + behavioral poisoned-environment test; 6-10 construction-no-candidate registry description corrected (claimed an emitter that does not exist \u2014 now marked RESERVED); round-7 real 6-3 closed with a real-CLI pass-count test (50 dispatch lines -> passes 0, handler may not name the file); check_acceptance now VERIFIES the forcing tests behavior-class rows name (the instrument that could not catch unenforced closures now refuses an unenforced row \u2014 and immediately flagged four of my own rows, which gained their pointers); sitting-sheet gate count corrected 17->24; plan-body knob spelling annotated with the suffixed authority", "status": "landed"}
{"phase": "2.3", "axis": "4", "kind": "delete", "subject": {"path": "factory_core/qualification.py"}, "note": "behavioral qualification deleted (founder-ratified 18b8c02bc10c: 'By all means. We can recover it from the repo if it's a mistake' \u2014 git history retains module + tests, dogfood-round historical records unrewritten). The only machinery admitting LLM-authored probe evidence as gate input; kills the channel-audit Goodhart flagship at birth (wall clock inside the qualification digest) and moots two Phase-3 collapse rows. instruction_control.py (host-owned byte admission) REMAINS the boundary; state_qualification stays uniform build-env-tier by construction. CLI qualify subcommand and both test files (322+257 lines) deleted with it; CLAUDE.md layout bullet removed with make check-doctrine evidenced green", "status": "landed"}
{"phase": "2.3", "axis": "4", "kind": "delete", "subject": {"path": "tests/test_qualification.py"}, "note": "deleted with factory_core/qualification.py (2.3) \u2014 the probe-evidence admission suite has nothing left to test; recoverable from git history per the founder's ruling", "status": "landed"}
{"phase": "2.3", "axis": "4", "kind": "delete", "subject": {"path": "tests/test_qualification_cli.py"}, "note": "deleted with the CLI qualify subcommand (2.3); recoverable from git history per the founder's ruling", "status": "landed"}
{"phase": "3.1", "axis": "3", "kind": "partial delete", "subject": {"path": "factory_core/manifest.py"}, "note": "Phase 3 change 1: the whole-history re-parse-and-verify per ledger APPEND is deleted \u2014 append now re-addresses only the tail record (suffix continuity) with the caller-held expected_head as the primary guarantee. Whole-chain verification is NOT demoted: verified_entries keeps its mandatory firing paths on every load/transition/rebuild read, forcing-tested by tests/test_manifest_ledger.py::test_append_is_suffix_scoped_and_reads_catch_the_mid_chain_edit (both halves: append extends past a mid-chain edit BY DESIGN, the read path refuses the whole history) and ::test_append_refuses_a_tampered_tail", "status": "landed"}
{"phase": "3", "axis": "3", "kind": "note", "subject": {"path": "factory_runtime/instruction_control.py"}, "note": "collapse points instruction_control.py:563 (whole-doctrine source_digest) and :451+cli.py:1052 (wall clock in the directive-contract digest) are MOOTED by 2.3: the only consumer with invalidation semantics was qualification's prompt_digest = digest_obj(whole role contract), deleted with the module. The surviving consumer (runner.py:710 checkpoint containment) uses source_digest as ANCHORING \u2014 binding the exact doctrine file the checkpoint pinned is correct semantics, not blanket invalidation \u2014 so no rebind is needed and none is performed; the per-role instructions_digest field remains available for future consumers", "status": "landed"}
{"phase": "3.2", "axis": "3", "kind": "partial delete", "subject": {"path": "factory_runtime/state.py"}, "note": "Phase 3 change 2: the full derive walk per READ is deleted for construction-state runs \u2014 head-pinned load trusts a projection whose self_digest (digested body INCLUDES ledger_head) verifies and whose ledger tail re-addresses to that head. One mismatch behavior, signal-emitting: full-chain verification fires (the mandatory load-mismatch path), the projection is re-derived, repaired, and the mismatch persisted to projection-repairs.jsonl \u2014 recovery is never silence (both halves forcing-tested). DISCOVERED COUPLING, scoped accordingly: the derive walk is also the carrier of replay-time cryptographic re-verification (preview evidence envelopes, retained adversarial review, dependency bytes) from VALIDATING onward, so the fast path is state-scoped to pure-recomputation states and every verification-bearing state always takes the full walk (tests/test_runtime_state.py::test_fast_path_never_covers_verification_bearing_states). Legacy unpinned projections keep the exact prior equality semantics so retained released runs replay untouched SECOND discovered hold: the every-load obligation invariant \u2014 the fast path verifies the TAIL transition's retained obligation set/report bytes against the digests its ledger entry spent (tests/test_transition_obligations.py::test_tampered_retained_obligation_report_breaks_authoritative_load holds on the fast path); deep-history obligation re-derivation stays on the full-walk paths.", "status": "landed"}
{"phase": "3.4", "axis": "3", "kind": "de-rate", "subject": {"path": "factory_core/build_plan.py"}, "note": "Phase 3 change 4: build-plan-pattern-catalog-mismatch DELETED (redundancy removal, claimed honestly — every step's pattern_digest is already checked against the catalog entry it names); build-plan-phase-artifacts-mismatch blanket map equality REPLACED by per-edge supersedes resolution (new host code, labeled): verify_intent_provenance resolves a stale backreference to the current item only via the exact declared prior-authority quadruple in the current item's signed supersedes set, and _canonicalize threads resolved references through coverage AND oracle links; state.py whole-generation-map equality replaced by per-axis comparison naming the axis (target-manifest-source carries the signal knobs by construction — a re-signed raised-deadline ABI fails that named axis and disarms nothing). The plan's three forcing tests land verbatim: tests/test_build_plan.py::test_resign_with_declared_supersedes_keeps_the_plan_ready_end_to_end, ::test_changed_canonical_statement_drifts_the_plan, ::test_oracle_expectation_change_cannot_be_masked_by_a_rebind", "status": "landed"}
{"phase": "4.1a", "axis": "7", "kind": "add", "subject": {"path": "tests/test_trust_root.py"}, "note": "4.1 trust root, first slice: (1) require_operator_owned_trust_root — the enrollment roster refuses to load from inside the runs root (an agent-adjacent surface where a lane could stage the roster governing its own authority), wired at cli._load_workflow, forcing-tested both directions; (2) wrap_json gains forbidden_signer_public_keys — the host signing seam handed an enrolled-human key REFUSES to mint and removes the envelope (human keys are minted/used only outside the host process), threaded at both workflow-scoped signers (orchestrator preview, repair brief) via human_public_keys(policy), proven end-to-end against the real tessera binary incl. nothing-forged-survives-on-disk", "status": "landed"}
{"phase": "4.1b", "axis": "7", "kind": "demote", "subject": {"path": "factory_runtime/workflow.py"}, "note": "4.1b first half — PHASE RATIFICATION goes single-seat (founder-ratified demotion of LLM signatures to provenance): the human receipt is the only authority; the Validator receipt is verified attribution (signature, enrolled principal, exact subject) whose NONCE IS DROPPED — no replay ceremony for a non-authority, no second disclosure mechanism on top of the provenance column. state.py _RATIFICATION_RECEIPT_ROLES shrinks to ('human',) with validator keys TOLERATED via _RECEIPT_ATTRIBUTION_ROLES so dual-ratified history replays green (nonce-count validators accept 0/1/2 phase nonces across the three ledger generations — v4 compat suite green). Both repair-brief signer-is-causal-Validator AUTHORITY checks deleted (write path and replay twin) — which agent signed cannot gate the repair; enrolled-agent attribution verification remains. Remaining 4.1b half (acceptance-catalog activation, test-change authorization, evidence_plane preview envelope, evidence-bundle signer check) next slice", "status": "landed"}
{"phase": "4.1b", "axis": "7", "kind": "demote", "subject": {"path": "factory_runtime/acceptance_obligations.py"}, "note": "4.1b second half — the remaining three Validator-authority sites demoted to attribution: acceptance-catalog activation (validator receipt verified, nonce never consumed, orchestrator records only the human nonce), test-change authorization (authority_nonces = human only; reservation file holds one nonce with dual-history shape tolerated; the human/Validator KEY-DISTINCTNESS check deleted — distinctness between an authority seat and an attribution seat gates nothing; acceptance_obligations 875-921 public-key-distinctness hole MOOTED, noted not patched), preview evidence envelope (evidence_plane fourth site — signature verified as attribution, authority lives in host-verified evidence + human seats). The host-minted-then-host-verified evidence-bundle signer check DELETED (the host signed the envelope one call above; verifying its own signature proved nothing). state.py nonce counts accept dual-history extras per activation on write and derive paths. Doctrine swept: CLAUDE.md dual-ratification lines updated, make check-doctrine evidenced green", "status": "landed"}
{"phase": "4.1c", "axis": "7", "kind": "add", "subject": {"path": "factory_runtime/transition_admission.py"}, "note": "TRANSITION_ADMISSION begins, built INCREMENTALLY axis-by-axis (stated strategy: each admission axis moves from the write-path elif chain + derive twin into one schema-version-keyed FROZEN row consumed by both paths, instead of betting the state machine on a single rewrite). First migrated axis: authority-nonce counting — the extraction immediately EXPOSED a live write/derive drift (write counted INTAKE only; derive counted TRA-or-INTAKE), the exact defect class the table closes. Forcing tests: frozen-row digest pin both directions (edit -> red, delete -> red), consume-the-table poisoning proof (both transition() and rebuild_projection() refuse through the one poisoned answer — neither path carries a private twin), the drift-closed unification, and generation tolerances", "status": "landed"}
{"phase": "4.1d", "axis": "5", "kind": "add", "subject": {"path": "factory_runtime/preflight.py"}, "note": "the 0.4 mechanical consequence lands: the signal-deadline condition (pass-index >= frozen deadline, no NO-relevant signal, residual blockers present) is a HOST REFUSAL of the next BUILDING/model admission at the orchestrator's dispatch-preflight door — never only a pager. SignalDeadlineFacts + probe_signal_deadline (passes from the verified ledger, deadline from the frozen blob, NO-relevance from the same committed baseline mirror the watchdog consumes, blockers from the retained readiness snapshot); tri-state per input (could-not-check disclosed loud, never green); healthy-green exemption noted, never silent; wired at orchestrator build_and_validate + the CLI preflight door. Stated honestly in the code comment: the BUILDING row of the full transition table absorbs this when that migration reaches it — until then this preflight IS the admission. Forcing tests: the plan's named pair (expired refuses with the exact code pass-N-of-M; healthy long green run admits with the exemption note) plus unverifiable-is-loud and signal-satisfies", "status": "landed"}
{"phase": "4.1", "axis": "7", "kind": "add", "subject": {"path": "factory_runtime/transition_admission.py"}, "note": "4.1 remainders: (1) the LLM-entry-row RULE lands as a mechanical contract — admission rows gain admits_external_bytes + named_validator, and a forcing test enumerates every row asserting a byte-admitting row names a validator resolvable to a callable in ADMISSION_VALIDATORS (no current row admits bytes; the first one that does cannot land unvalidated) and that a validator name on a non-admitting row is refused as dead configuration; (2) intake authority-reachability: a roster with ZERO enrolled humans is now a preflight HARD NO (preflight-authority-unreachable — every receipt signer unresolvable, __DONE__ provably unreachable), upgraded from the Critical-scoped capacity disclosure which silently admitted a nothing-can-ever-ratify run", "status": "landed"}
{"phase": "4.2-7", "axis": "5", "kind": "de-rate", "subject": {"path": "factory_core/manifest.py"}, "note": "4.2 change 7 first half (degrade-never-wedge): the O_EXCL ledger-append sentinel becomes a CRASH-RELEASED fcntl flock — a SIGKILLed appender releases the lock with its process, so the crash case repairs itself (forcing test: present-but-unflocked lock file appends straight through; a HELD lock still refuses — real exclusion is not a wedge). The lock file deliberately persists (unlinking a flock file reintroduces the classic inode race), so ledger.jsonl.lock LEFT the liveness guard-residue set (its existence signals nothing now); resources.guard and run-transition.guard remain wedge evidence until their own conversions. Second half (receipted factory ledger-unlock for the remaining sentinels + factory chain-repair, shared operator-key shape) lands with 4.2 change 2", "status": "landed"}
{"phase": "4.2-2+7", "axis": "5", "kind": "add", "subject": {"path": "factory_runtime/repair_ceremony.py"}, "note": "the receipted repair ceremonies — the ONLY exits from a wedge, each ONE bounded operator action (property iii pinned by forcing test). factory chain-repair: consumes a PRE-SIGNED adjudication (4.1a: human keys never enter the host — the operator signs out-of-band, the host verifies against enrolled-human keys and applies), quarantines the named offending suffix (nothing destroyed), installs the signed license at receipts/repairs/<hash>.tessera.json which _load_chain REQUIRES before accepting a shortened chain (unreceipted surgery is itself a wedge); post-repair the chain loads and append-after-repair grounds. factory ledger-unlock: liveness-checked (live pid = real exclusion, refuses), releases the sentinel guard with bytes + signed record retained under evidence/unlocks/ (recovery is itself a signed fact, never a bare removal). End-to-end forcing tests against the REAL tessera binary incl. the non-enrolled-signer refusal; gates.tsv rows RC1/RC2 with denial probes", "status": "landed"}
{"phase": "4.2-2b", "axis": "5", "kind": "add", "subject": {"path": "harness/receipt.sh"}, "note": "4.2 change 2 append-time half: duplicate receipt-id REJECTION inside the existing fcntl-locked critical sections of all three producers (receipt.sh, mutate.sh, flake.sh) — the R5 wedge is refused at the WRITER instead of discovered at the next promote; forcing test executes the guard verbatim against a colliding chain (nonzero exit, zero rows appended) plus per-producer structural assertion. Consumer side: dispatcher.counts() de-rated from line count to unique-VALID-id counting with a chain_rows_unverifiable event on malformed/duplicate rows (display metric only — the pass unit stays at the CLI door and full digest verification stays on the promotion seam's mandatory path, stated in the docstring)", "status": "landed"}
